extends layout.pug

block body
  section
    .tree-front
      div(alt=(__(title) || ''))!= svg('images/principles/' + title.toLowerCase().split(' ').join('-').split('/').join('-').split('(').join('').split(')').join('').split('!').join('') + '.svg')
      p= __(title) || ''
    != markdown || ''
